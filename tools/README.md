# Tools

Operational tooling lives here instead of at the repository root.

- `scripts/` contains local developer and container helpers.
- `devops/` is reserved for reusable environment, release, and platform automation.
