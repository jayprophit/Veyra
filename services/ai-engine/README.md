# AI Engine

Local-first AI integration boundary. The active implementation talks to a local Ollama runtime and keeps all model calls behind one API contract so future agents, summarizers, and tool policies do not bind directly to one provider.
