# Crawler

Readable-content extraction for research workflows. This service starts with deterministic HTTP fetching plus pagination and is the foundation layer beneath richer adapters such as Crawl4AI.
