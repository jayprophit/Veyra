apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "veyra.fullname" . }}-config
  labels:
    {{- include "veyra.labels" . | nindent 4 }}
data:
  ENVIRONMENT: {{ .Values.global.environment | quote }}
  DATABASE_URL: "postgresql://{{ .Values.postgresql.auth.username }}:{{ .Values.postgresql.auth.password }}@{{ include "veyra.fullname" . }}-postgresql:5432/{{ .Values.postgresql.auth.database }}"
  REDIS_URL: "redis://{{ include "veyra.fullname" . }}-redis-master:6379/0"
  CORS_ORIGINS: "*"
