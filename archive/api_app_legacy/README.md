# Legacy API App Catalog

This directory preserves the previous backend prototype catalog that used to live under `services/api-gateway/app`.

It is not imported by the active API gateway and should not be treated as production-ready code. Reuse ideas selectively when building the private pre-public workstreams, but implement new behavior in the canonical services rather than reactivating this entire tree.
